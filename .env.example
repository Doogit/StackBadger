# StackBadger — test account credentials
# Copy to .env and fill in. These four values are the ONLY ones required.
# All provider config (Supabase URL + anon key, Clerk FAPI host, Firebase API
# key, etc.) is auto-discovered from the target site's JS bundle at runtime.

PENTEST_USER_A_EMAIL=
PENTEST_USER_A_PASSWORD=
PENTEST_USER_B_EMAIL=
PENTEST_USER_B_PASSWORD=

# --- Target confirmation + authorization gates (required for any REMOTE target) ---
# run.sh refuses to scan any non-localhost host (read-only mode included)
# unless BOTH values below EXACT-match the host of the EFFECTIVE TARGET — the
# URL you pass to run.sh, or TARGET_BASE_URL if you set that override (it wins
# over the CLI arg; the gate confirms exactly what gets scanned).
# Set the bare host: lowercase, no scheme, no port, and NO subdomain
# cross-match (api.example.com != example.com). It is the CLI/override host,
# NOT a post-redirect host — if your site redirects apex -> www and you want to
# test www, pass the www URL (./run.sh https://www.example.com) and set the
# values to www.example.com. --yes does NOT bypass either gate; localhost /
# 127.0.0.1 / [::1] are exempt.
#
# CONFIRM_TARGET — "this is the right host" (typo / wrong-environment guard).
#   Example: for ./run.sh https://staging.example.com set:
# CONFIRM_TARGET=staging.example.com
#
# CONFIRM_AUTHORIZED — "a human affirmed authorization to test this host".
#   Must be set by the site OWNER out-of-band (not by an agent following
#   LAUNCH.md). Only set this for systems you own or are explicitly
#   authorized, in writing, to test.
# CONFIRM_AUTHORIZED=staging.example.com

# --- Optional ---
# Supabase project SERVICE-ROLE key — required ONLY by provision_accounts.py /
# teardown.py (the GoTrue Admin API accepts nothing else). Dashboard ->
# Project Settings -> API keys. Treat it like a root password: never commit
# it, never paste it into a chat/transcript. The scripts never echo it.
# SUPABASE_SERVICE_ROLE_KEY=
# Supabase Management API token — required ONLY for --branch mode. It can NOT
# call the GoTrue Admin API; provision_accounts.py uses it solely as an
# optional way to FETCH the service-role key when that is unset.
# SUPABASE_ACCESS_TOKEN=
# Written by provision_accounts.py (used by teardown to delete by ID):
# PENTEST_USER_A_ID=
# PENTEST_USER_B_ID=
# Internal-endpoint authorization secret — for internal-endpoint probes.
# PENTEST_INTERNAL_SECRET=
# Provider config overrides (normally auto-discovered; set only to override):
# SUPABASE_PROJECT_URL=
# SUPABASE_ANON_KEY=
# CLERK_FAPI_HOST=
# FIREBASE_API_KEY=
