# Runtime artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
report.json

# Internal planning docs (local only — not for publication)
docs/plans/

# Compliance evidence/attestations (local only — not for publication)
docs/compliance/

# Generated files
reports/output/
reports/evidence/
reports/pytest-report.json
reports/zap-report.json
*.asvs-markers.json
fixtures/records_oversized.csv
zap/automation-plan.runtime.yaml

# Environment
.env
*.env
!zap/zap-api.env.example
.venv/
venv/
.context/

# Real per-target profiles (keep example templates tracked)
profiles/*.yaml
!profiles/*-example.yaml

# Engagement authorization (keep the example template tracked)
AUTHORIZATION.md
roe/

# Local agent workspace config (local only — not for publication)
.claude/
CLAUDE.md
